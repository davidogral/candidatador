"""Document vault: resumes (multiple versions), cover letters, certificates, courses..."""

from candidatador.documents.vault import add_document, extract_text, pick_resume

__all__ = ["add_document", "extract_text", "pick_resume"]
